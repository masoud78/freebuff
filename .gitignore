# Dependencies
node_modules/

# Build output
dist/
.vite/
coverage/

# Personal overrides (never committed)
.env.local

# Stray debug workspaces under apps (never committed)
apps/**/workspace/

# Freebuff tooling & preview state (regenerated locally — not project data)
.freebuff/

# Logs
*.log
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Local runtime data (SQLite database, WAL, credentials) — never committed
workspace/system/

# Uploaded audio files (runtime data) — never committed
workspace/audio/
